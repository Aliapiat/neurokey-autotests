# Copy to .env and fill in real values. NEVER commit the real .env.
# Both files are gitignored.

# Учётка для реальных логинов (E2E против dev/prod)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Playwright
HEADLESS=false
BROWSER=chromium
SLOW_MO=0
DEFAULT_TIMEOUT=30000
